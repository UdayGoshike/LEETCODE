class Solution {
public:
    bool stoneGame(vector<int>& piles) {
        // int l = 0;
        // int r = piles.size() - 1;
        // int a = 0;
        // int b = 0;
        // bool f = true;
        // while (l <= r) {
        //     if (f == true) {
        //         if (piles[l] >= piles[r]) {
        //             a += piles[l];
        //             l++;
        //         } else {
        //             a += piles[r];
        //             r--;
        //         }
        //         f=false;
        //     } else if (f == false) {
        //         if (piles[l] >= piles[r]) {
        //             a += piles[l];
        //             l++;
        //         } else {
        //             a += piles[r];
        //             r--;
        //         }
        //         f=true;
        //     }
        // }
        // return a>b;
        return true;
    }
};